<Plan>
**Backend (FastAPI):**
MongoDB models: Device, Room, AlertRule, ConsumptionLog, AppConfig
CRUD endpoints for devices/rooms/alerts
Mock state changes + simulated consumption history
Config endpoint for Flask server URL (multi-vendor abstraction: tapo/shelly/sonoff/generic)
Seed data: sample house with 5 rooms and ~10 devices
**Frontend (Expo, .tsx):**
_layout.tsx: preserve icon fonts, add Stack
index.tsx: Dashboard with 3D isometric house map (react-three-fiber) + bento-style sidebar with rooms, status strip, quick device controls
device/[id].tsx: detail screen with state, consumption chart (svg), alert rules
devices.tsx: device list, add/remove with vendor selector
settings.tsx: Flask URL config, theme
alerts.tsx: alerts history
Dark luxury theme per design tokens, lucide icons, glass cards
API client to FastAPI backend
**Packages to install:** three, @react-three/fiber, expo-gl, react-native-svg, lucide-react-native, @react-three/drei

** Install **
$ cd /app/frontend && yarn expo install three @react-three/fiber expo-gl react-native-svg lucide-react-native 2>&1 | tail -30